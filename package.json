{
  "name": "agentpulse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile human-in-the-loop control for Claude Code and Codex.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s bridge -p 'test_*.py'",
    "secrets": "node scripts/generate-secrets.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "otpauth": "9.5.2",
    "qrcode": "1.5.4",
    "web-push": "3.6.7"
  }
}
